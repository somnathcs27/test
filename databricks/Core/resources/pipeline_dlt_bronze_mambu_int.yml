resources:
  pipelines:
    pipeline_dlt_bronze_mambu_int:
      name: dlt_bronze_mambu_int
      clusters:
        - label: default
          autoscale:
            min_workers: 1
            max_workers: 5
            mode: ENHANCED
      libraries:
        - notebook:
            # This path looks wrong - should be relative ../src - but there's no more correct path to point it at in the main branch yet.
            path: /Repos/14793@lhbs.net/databricks/1.
              bronze_merges/Mambu/nb_dlt_bronze_int_data_model
      target: bronze_mambu_int
      development: true
      catalog: ${bundle.target}_catalog