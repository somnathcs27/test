resources:
  jobs:
    run_spark_declarative_pipeline:
      name: run_spark_declarative_pipeline_${bundle.target}

      tasks:
        - task_key: refresh_pipeline
          pipeline_task:
            pipeline_id: ${resources.pipelines.spark_declarative_pipeline.id}